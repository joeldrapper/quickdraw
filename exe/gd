#!/usr/bin/env ruby
# frozen_string_literal: true

require_relative "../lib/green_dots"

SUCCESS_EMOJI = %w[💃 🕺 🎉 🎊 💪 👏 🙌 ✨ 🥳 🎈]

require "tty-tree"
require "tty-box"
require "tty-table"
require "pastel"
require "etc"
require "concurrent"

GreenDots.run(
	number_of_processes: 1, # Etc.nprocessors,
	directory: ARGV[0] || "test",
	test_files: Dir.glob("./**/*.test.rb").reject { |f| f.start_with? "#{@directory}/support/" }
)
